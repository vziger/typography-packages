# Shared analyzer config for all packages in the monorepo.
# Each package includes this file via a relative `include:`.
include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
