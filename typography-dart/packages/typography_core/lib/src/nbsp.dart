/// Non-breaking space characters used by typography rules.
library;

/// Regular non-breaking space, `U+00A0`.
///
/// Used between short words and before an em dash, and between a numeral and
/// its dependent noun.
const String nbsp = ' ';

/// Narrow (half) non-breaking space, `U+202F`.
///
/// Used between a number and a `%` sign or a currency symbol.
const String narrowNbsp = ' ';
