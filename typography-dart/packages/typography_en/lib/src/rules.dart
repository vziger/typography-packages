/// English micro-typography rules.
///
/// Minimal scaffold: currently empty. This file exists to show the shape a new
/// language package takes — add `Rule`s here and they are applied in order by
/// the `en()` facade via `applyRules`.
library;

import 'package:typography_core/typography_core.dart';

/// Ordered list of English typography rules (empty placeholder for now).
final List<Rule> englishRules = <Rule>[];
